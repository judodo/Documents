graph TD
    A[開始] --> B{是否登入?}
    B -->|是| C[顯示主頁面]
    B -->|否| D[顯示登入頁面]
    C --> E[選擇功能]
    D --> F[輸入帳號密碼]
    F --> G{驗證成功?}
    G -->|是| C
    G -->|否| H[顯示錯誤訊息]
    H --> D
    E --> I[結束]
